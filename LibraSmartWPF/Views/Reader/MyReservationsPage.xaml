<Page x:Class="LibraSmartWPF.Views.Reader.MyReservationsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:viewmodels="clr-namespace:LibraSmartWPF.ViewModels"
      Title="MyReservationsPage">

    <Page.DataContext>
        <viewmodels:MyReservationsViewModel />
    </Page.DataContext>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="Мои бронирования"
                  Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                  Margin="0,0,0,24"/>

        <!-- Кнопки действий -->
        <StackPanel Grid.Row="1"
                   Orientation="Horizontal"
                   Margin="0,0,0,16">
            <Button Command="{Binding RefreshCommand}"
                   Style="{StaticResource MaterialDesignOutlinedButton}"
                   Margin="0,0,8,0">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Refresh"
                                           VerticalAlignment="Center"
                                           Margin="0,0,8,0"/>
                    <TextBlock Text="Обновить" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <Button Command="{Binding CancelReservationCommand}"
                   Style="{StaticResource MaterialDesignOutlinedButton}"
                   Foreground="{DynamicResource MaterialDesign.Brush.ValidationError}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Cancel"
                                           VerticalAlignment="Center"
                                           Margin="0,0,8,0"/>
                    <TextBlock Text="Отменить бронирование" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </StackPanel>

        <!-- Список бронирований -->
        <materialDesign:Card Grid.Row="2">
            <Grid>
                <!-- Индикатор загрузки -->
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                            Value="0"
                            IsIndeterminate="True"
                            Width="50"
                            Height="50"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <!-- DataGrid с бронированиями -->
                <DataGrid ItemsSource="{Binding Reservations}"
                         SelectedItem="{Binding SelectedReservation}"
                         AutoGenerateColumns="False"
                         CanUserAddRows="False"
                         CanUserDeleteRows="False"
                         IsReadOnly="True"
                         SelectionMode="Single"
                         materialDesign:DataGridAssist.CellPadding="8"
                         materialDesign:DataGridAssist.ColumnHeaderPadding="8">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Книга"
                                          Binding="{Binding BookTitle}"
                                          Width="*"
                                          MinWidth="200"/>
                        <DataGridTextColumn Header="Автор"
                                          Binding="{Binding BookAuthor}"
                                          Width="*"
                                          MinWidth="150"/>
                        <DataGridTextColumn Header="Жанр"
                                          Binding="{Binding GenreName}"
                                          Width="150"/>
                        <DataGridTextColumn Header="Библиотека"
                                          Binding="{Binding LibraryName}"
                                          Width="200"/>
                        <DataGridTextColumn Header="Дата бронирования"
                                          Binding="{Binding ReservationDate, StringFormat='dd.MM.yyyy HH:mm'}"
                                          Width="150"/>
                        <DataGridTextColumn Header="Статус"
                                          Binding="{Binding StatusDisplay}"
                                          Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>
    </Grid>
</Page>
